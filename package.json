{
  "name": "sunny-crypto-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "build": "mkdir -p dist && cp -r src/* dist/ && cp package.json dist/",
    "test": "vitest"
  },
  "dependencies": {
    "twitter-api-v2": "^1.15.2",
    "node-schedule": "^2.1.1",
    "date-fns": "^3.3.1",
    "dotenv": "^16.4.5",
    "axios": "^1.6.7",
    "@supabase/supabase-js": "^2.39.7",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "vitest": "^1.3.1"
  }
}